# conda env create --file neuralangelo.yaml
name: neuralangelo
prefix: /root/autodl-tmp/neuralangelo_env
channels:
  - pytorch
  - conda-forge
dependencies:
  # general
  - gpustat
  - gdown
  #- cudatoolkit-dev # maybe no need to install if CUDA driver is already installed on the system
  - cmake
  # python general
  - python=3.8
  - pip
  - numpy
  - scipy
  - ipython
  - jupyterlab
  - cython
  - ninja
  - diskcache
  # >>>>> TODO: need to install following 3 pkgs
  - pytorch==1.7.1
  - torchvision==0.8.2
  - cudatoolkit==11.0
  #>>>>> Uncomment the following pip line <<<<<
  #- pip:
  #  - -r requirements.txt
#prefix: /research/d1/msc/txyang23/SOFTWARE/miniconda/env/neuralangelo
